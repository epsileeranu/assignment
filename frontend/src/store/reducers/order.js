// import * as actionTypes from '../actions/actionTypes';


const orderReducerDefaultState = {

};
export const orderReducer = (state = orderReducerDefaultState, action) => {
  switch(action.type){
    default: return state;
  }
};

